# Ultra-review remediation

All 14 findings from the review are addressed in PCF 0.2: provider marker placement and model profiles; stable cache partitions; structured tool history; read-only TTL inspection; stable Platt fitting; held-out calibration eligibility; nullable confidence and real fallback behavior; cache-write pricing; descriptor/byte-compatibility separation; strict schemas and finite numbers; provider boundary/authority handling; RFC 8785 canonicalization; input/metrics validation; and provenance-preserving external documents.

Intentional claim limits: closed-provider warmth is unknown before execution, synthetic validation is not a production-quality certificate, and no component claims cross-family raw KV transfer.
